<p>This is the teacher's lounge of admin pages.</p>
<p>As an admin you can delete & update descriptions of ANY busker, not just your own. With great power...</p>

<h1><%= user.username %>'s Profile</h1>
<div class="row">
    <div class="col s12 l4">
        <img class="img-responsive" alt="user image" src="<%= user.photoUrl %>">
    </div>
    <div class="col s12 l8">
        <h2><%= user.username %></h2>
        <p><strong>Full Name: <%= user.firstname %> <%= user.lastname %></strong></p>
        <p><strong>Admin Status: <%= user.admin ? 'Yes' : 'No' %></strong></p>
    </div>
</div>
